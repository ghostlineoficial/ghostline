import { Container } from '@/components/ui/Container';
import { SectionTitle } from '@/components/shared/SectionTitle';
import { Button } from '@/components/ui/Button';
export default function SocietyPage(){return <Container><SectionTitle eyebrow="Ghost Society" title="Acesso antes de todo mundo"/><p className="mt-6 max-w-2xl text-muted">Bastidores, drops antecipados, comunidade exclusiva e Ghost Signal no WhatsApp.</p><Button className="mt-8" disabled>Em breve</Button></Container>}
